# Puerto dinmico para Render
server.port=${PORT:8080}

# Configuracin de base de datos
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:6543/postgres?prepareThreshold=0&ssl=true&sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.mfdvphvbhftpvkvlrejo}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:uyDSptQePOlA7VED}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuracin de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Ajustes de tiempo de espera
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10

# Configuracin de subida de archivos
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Seguridad JWT
jwt.secret=${JWT_SECRET:GemaInventoryProfessionalSecurityKey2026FixedSecretForDevelopment}

# Reporte de errores
server.error.include-message=always

# Configuracin de Resend (Email)
resend.api.key=${RESEND_API_KEY:re_9eMrZAns_6sWzaq87V8V5J2NaF21edJDM}

# Logging para depuracin
logging.level.org.postgresql=INFO
logging.level.com.zaxxer.hikari=INFO
logging.level.org.hibernate.SQL=INFO

# Supabase Storage Configuration
supabase.url=${SUPABASE_URL:https://mfdvphvbhftpvkvlrejo.supabase.co}
supabase.key=${SUPABASE_KEY}
